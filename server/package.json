{
  "name": "card-game",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --outdir=dist --platform=node --format=cjs",
    "dev": "esbuild main.ts --bundle --outdir=dist --platform=node --format=cjs --watch",
    "start": "node .", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@types/ws": "^8.5.14",
    "@card-game/types": "workspace:*",
    "esbuild": "^0.25.0"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  }
}
